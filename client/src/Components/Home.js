import React from 'react'
import axios from 'axios'
import {Card, Header, Button, Icon} from 'semantic-ui-react'
import {link} from 'react-router-dom'

const Home = (props) => {
return(
  <Header as='h1'>SPACEMALL</Header>
)
}

export default Home